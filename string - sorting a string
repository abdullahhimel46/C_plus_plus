#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    getline(cin, s);

    //ascending sort
    sort(s.begin(), s.end());
    cout << s << endl;

    //descending sort
    sort(s.begin(), s.end(), greater<char>());
    cout << s << endl;

    return 0;
}

//Link: https://codeforces.com/problemset/problem/1/A
#include <bits/stdc++.h>
using namespace std;
int main()
{
    cin.tie(0);
    long long int n, m, a;
    cin >> n >> m >> a;
    n = ceil(1.00 * n / a);
    m = ceil(1.00 * m / a);
    cout << n * m<<endl;

    return 0;
}
